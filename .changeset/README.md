# Changesets

This directory contains release notes for `@yielded/starlight-theme`. The landing
page is private and not published.

Create a changeset for consumer-visible theme changes, review the generated version
plan, and merge the automated version pull request to publish. See the release
runbook in `docs/TOOLCHAIN.md`.
