Code shown on the landing page, copied verbatim from each library:

- `sync.ts.txt` — sync `docs/src/content/docs/guide/getting-started.md`, "Define a shared contract"
- `auth.ts.txt` — auth `README.md`, `auth-server` region
- `agent.ts.txt` — effect-agent `README.md`, "A basic agent"

Update them when those examples change.
